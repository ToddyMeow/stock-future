# Runtime dependencies for live system (web + signal engine).
# Excludes dev/test (pytest), research (matplotlib/seaborn/jupyter), and optional vendor SDKs.
# Versions pinned to what's currently working on Mac dev env.

# Web server
fastapi==0.136.0
uvicorn==0.44.0
httpx==0.25.0

# Data manipulation
numpy==1.24.3
pandas==2.0.3
pytz==2023.3

# Database
SQLAlchemy==2.0.22
psycopg[binary]==3.3.3
psycopg2-binary==2.9.9
asyncpg==0.31.0

# Config / serialization
pydantic==2.13.2
python-dotenv==1.0.0
PyYAML==6.0.1
Jinja2==3.1.2

# Data vendor (RQData futures SDK)
rqdatac==3.4.7.8
